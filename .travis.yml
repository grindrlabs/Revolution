---
dist: trusty

language: python
python: 3.5.3
cache: { pip: true, directories: [eggs/] }

install: make setup

script: bin/test

notifications:
  email:
    on_success: never
    on_failure: never
