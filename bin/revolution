#!/usr/bin/env ruby
# frozen_string_literal: true

lib = File.expand_path('../lib', File.dirname(__FILE__))
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'revolution'

Revolution.run if $PROGRAM_NAME == __FILE__
